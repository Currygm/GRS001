let state;
function render() {
  $("raceList").innerHTML = state.races.length ? state.races.map(r => { const versions = r.archive?.versions || [], current = versions.find(v => v.version === r.archive?.currentVersion), challenge = r.challenge; return `<article class="panel race-card"><span class="eyebrow">${statusLabel(r.status)} · ${r.raceId}</span><h2>${escapeHtml(r.manifest.disclosure.title)}</h2><p>${formatDateTime(r.startsAt)} 至 ${formatDateTime(r.endsAt)} · ${remainingText(r)}</p><p>结构化赛题：${challenge ? `v${challenge.version} · ${escapeHtml(challenge.title)}` : "待配置"} · 参加：${r.participantCount} · 提交：${r.metadata.submissionStatus}</p>${challenge ? `<div class="audit"><span>任务描述</span><strong>${escapeHtml(challenge.description)}</strong><div>提交要求：${escapeHtml(challenge.submissionRequirements || "无")} · 评审标准：${escapeHtml(challenge.evaluationCriteria || "未说明")} · 补充说明：${escapeHtml(challenge.notes || "无")}</div></div>` : ""}<p>当前长期归档：${current ? `v${current.version}` : "未发布"} · 历史版本：${versions.length} · 私人证书：${r.certificateCount}</p>${versions.map(v => `<div class="audit"><span>v${v.version}</span><strong>${formatDateTime(v.publishedAt)}</strong><div>海报 SHA256 ${v.posterSha256}</div></div>`).join("")}</article>`; }).join("") : `<div class="empty">暂无赛事</div>`;
  $("proofClaims").innerHTML = state.proof.claims.map(i => `<div class="claim ${i.passed ? "pass" : "fail"}"><strong>${i.passed ? "PASS" : "FAIL"} · ${escapeHtml(i.name)}</strong><p>${escapeHtml(i.evidence)}</p></div>`).join(""); $("organizerFiles").innerHTML = filesHtml(state.proof.organizerFiles); $("aryFiles").innerHTML = filesHtml(state.proof.aryFiles);
  $("auditLog").innerHTML = state.audits.length ? state.audits.map(l => `<div class="audit"><span>${new Date(l.at).toLocaleTimeString()}</span><strong>${escapeHtml(l.actor)}</strong><div>${escapeHtml(l.action)} · ${escapeHtml(l.target)}</div></div>`).join("") : `<div class="empty">暂无审计</div>`;
}
async function refresh() { state = await api("/api/state"); render(); }
$("runProof").onclick = async () => { await api("/api/proof/run", { method: "POST" }); toast("扫描完成"); await refresh(); }; $("reset").onclick = async () => { await api("/api/demo/reset", { method: "POST" }); toast("Demo 已清空"); await refresh(); }; refresh(); setInterval(refresh, 5000);
