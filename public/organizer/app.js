let state,selectedRaceId;const selected=()=>state.races.find(r=>r.raceId===selectedRaceId);
function render(){ $("raceList").innerHTML=state.races.length?state.races.map(r=>`<button class="race-select ${r.raceId===selectedRaceId?"secondary":""}" data-race-id="${r.raceId}">${escapeHtml(r.manifest.disclosure.title)} · ${statusLabel(r.status)} · ${r.datasetUploaded?"赛题已上传":"待上传赛题"}</button>`).join(""):`<div class="empty">尚未创建赛事</div>`;const r=selected();$("detail").classList.toggle("hidden",!r);if(!r)return;const m=r.manifest.disclosure;$("detailTitle").textContent=m.title;$("titleInput").value=m.title;$("summaryInput").value=m.summary;$("posterVisible").checked=m.posterVisible;$("downloadPermission").checked=r.downloadPolicy.enabled;$("extendEndsAt").min=toLocalInput(new Date(Math.max(Date.now(),new Date(r.endsAt).getTime())+60000));$("raceTimeStatus").textContent=`状态：${statusLabel(r.status)} · 开始：${formatDateTime(r.startsAt)} · 结束：${formatDateTime(r.endsAt)} · ${remainingText(r)}`;$("datasetStatus").textContent=r.datasetUploaded?"专属赛题已上传，可以下载。":"尚未上传赛题；Racer 可以参加和提交，但不能下载。";$("submissionStatus").textContent=`参加人数：${r.participantCount} · 提交状态：${r.metadata.submissionStatus} · 评审：${r.metadata.review?r.metadata.review.score:"未评审"}`;$("livePreview").innerHTML=`<div class="preview-card">${m.posterVisible?`<img src="/organizer/races/${r.raceId}/poster.svg?v=${r.manifest.version}">`:`<div class="empty">海报已撤回</div>`}<div><span class="eyebrow">${statusLabel(r.status)} · V${r.manifest.version}</span><h2>${escapeHtml(m.title)}</h2><p>${escapeHtml(m.summary)}</p><p>${escapeHtml(m.timeline)}</p></div></div>`;$("downloadTickets").innerHTML=r.tickets.length?r.tickets.map(t=>`<div class="audit"><span>${escapeHtml(t.status)}</span><strong>${escapeHtml(t.racerId)}</strong><div>${new Date(t.issuedAt).toLocaleTimeString()}</div></div>`).join(""):`<div class="empty">暂无票据</div>`;}
async function refresh(id=selectedRaceId){state=await api("/api/state");selectedRaceId=state.races.some(r=>r.raceId===id)?id:state.races[0]?.raceId;render();}
$("createRace").onclick=async()=>{const x=await api("/api/organizer/races",{method:"POST",body:JSON.stringify({title:$("createTitle").value,summary:$("createSummary").value,startsAt:new Date($("createStartsAt").value).toISOString(),endsAt:new Date($("createEndsAt").value).toISOString()})});toast("赛事已创建");await refresh(x.race.raceId);};$("raceList").onclick=e=>{const b=e.target.closest("[data-race-id]");if(b){selectedRaceId=b.dataset.raceId;render();}};$("saveDisclosure").onclick=async()=>{await api(`/api/organizer/races/${selectedRaceId}/disclosure`,{method:"POST",body:JSON.stringify({title:$("titleInput").value,summary:$("summaryInput").value,posterVisible:$("posterVisible").checked})});toast("披露已更新");await refresh();};
$("extendRace").onclick=async()=>{await api(`/api/organizer/races/${selectedRaceId}/extend`,{method:"POST",body:JSON.stringify({endsAt:new Date($("extendEndsAt").value).toISOString()})});toast("赛事结束时间已延长");await refresh();};
$("uploadDataset").onclick=async()=>{const f=$("datasetFile").files[0];if(!f)throw new Error("请选择 PDF");const r=await fetch(`/api/organizer/races/${selectedRaceId}/dataset`,{method:"POST",headers:{"Content-Type":"application/pdf","X-File-Name":encodeURIComponent(f.name),"X-File-Size":String(f.size)},body:f}),x=await r.json();if(!r.ok)throw new Error(x.error);$("datasetResult").textContent=`大小：${x.size} B\nSHA256：${x.sha256}`;toast("赛题已上传");await refresh();};$("saveDownloadPermission").onclick=async()=>{await api(`/api/organizer/races/${selectedRaceId}/download-permission`,{method:"POST",body:JSON.stringify({enabled:$("downloadPermission").checked})});toast("权限已更新");await refresh();};$("reviewSubmission").onclick=async()=>{await api(`/api/organizer/races/${selectedRaceId}/review`,{method:"POST",body:JSON.stringify({score:$("scoreInput").value,comment:$("commentInput").value})});toast("评审已保存");await refresh();};$("publishArchive").onclick=async()=>{await api(`/api/organizer/races/${selectedRaceId}/archive`,{method:"POST"});toast("公开回顾已发布");await refresh();};
const now=new Date(),endToday=new Date(now);endToday.setHours(23,59,0,0);$("createStartsAt").min=toLocalInput(now);$("createStartsAt").max=toLocalInput(endToday);$("createStartsAt").value=toLocalInput(new Date(now.getTime()+60000));$("createEndsAt").min=toLocalInput(new Date(now.getTime()+120000));$("createEndsAt").value=toLocalInput(new Date(now.getTime()+3600000));$("createStartsAt").onchange=()=>{$("createEndsAt").min=toLocalInput(new Date(new Date($("createStartsAt").value).getTime()+60000));};refresh();setInterval(()=>refresh(),5000);
